<%= form_for(@wazzup) do |f| %>
  <% if @wazzup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wazzup.errors.count, "error") %> prohibited this wazzup from being saved:</h2>

      <ul>
      <% @wazzup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :who %><br />
    <%= f.text_field :who %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
